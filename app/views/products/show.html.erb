Show page for Admin

<%= link_to image_tag(@product.image.url(:med)), @product.image.url %>

<%= @product.name %>
<br>
<%= @product.description %>
<br>
Delivery Date: Today! <%= @product.date_delivered %>

<%= button_to "Add to Cart"  %>
