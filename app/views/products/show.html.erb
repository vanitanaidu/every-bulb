
<h1> Daily Pick </h1>

    <%= link_to new_line_product_path do %>
      <%= image_tag(@product.image.url(:large)) %>
    <% end %>

    <%= render partial: "product", locals: {product: @product} %>
